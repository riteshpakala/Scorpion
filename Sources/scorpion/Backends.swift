//
//  Backends.swift
//  scorpion
//
//  Composition root: the executables decide which executor families exist. ScorpionKit
//  registers only the analytic `toy` backend itself.
//

import ScorpionKit

enum Backends {
    static func registerAll() {}
}
